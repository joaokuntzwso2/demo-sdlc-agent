openai>=1.50.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pytest>=8.3.0
httpx>=0.27.0
langgraph>=1.1,<2.0
langchain-openai>=1.2,<2.0
langchain-core>=1.3,<2.0

# Optional but recommended for WSO2 Agent Manager / OpenTelemetry demo tracing.
# Keep wrapt<2 because older LangChain instrumentation pulled by traceloop-sdk
# can fail with wrapt 2.x.
traceloop-sdk==0.51.1
wrapt<2